#include "interpolared_triangle_render.hxx"

interpolared_triangle_render::interpolared_triangle_render(canvas& buffer,
                                                           size_t  width,
                                                           size_t  height)
    : indexed_triangle_render(buffer, width, height){};
